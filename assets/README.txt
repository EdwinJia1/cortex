Place your fixed base images here.

For Level 1, add the PNG as:

  assets/level1_base.png  (1024x1024 recommended)

This file is used as the base scene (cat in a tree). All user edits in Level 1 will be composited on top of this image without calling external APIs.

